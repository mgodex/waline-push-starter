{
  "name": "waline-comment-system",
  "version": "1.0.0",
  "description": "Waline comment system for Tencent Cloud EdgeOne Pages",
  "main": "functions/comment.js",
  "scripts": {
    "build": "echo 'No build step required'",
    "deploy": "echo 'Deploy to EdgeOne Pages'"
  },
  "dependencies": {
    "@waline/vercel": "^1.31.9"
  },
  "edgeone": {
    "functions": {
      "comment": {
        "file": "functions/comment.js",
        "runtime": "nodejs18.x",
        "memory": 128,
        "timeout": 30
      }
    },
    "routes": [
      {
        "path": "/api/comment",
        "function": "comment"
      }
    ]
  }
}
